export enum SaleStatus {
    COMPLETED = "completed",
    RETURNED = "returned",
}

export enum BuyerType {
    MEMBER = "member",
    STAFF = "staff",
}

export enum PaymentMethod {
    CASH = "cash",
    CARD = "card",
    TRANSFER = "transfer",
}
