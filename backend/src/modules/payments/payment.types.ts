export enum PaymentMethod {
    CASH = "cash",
    CARD = "card",
    TRANSFER = "transfer",
}

export enum PaymentStatus {
    PENDING = "pending",
    PAID = "paid",
    CANCELLED = "cancelled",
    REFUNDED = "refunded",
}